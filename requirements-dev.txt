# Geliştirme + test bağımlılıkları (production'da kurulmaz)
-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2          # FastAPI TestClient
ruff==0.7.4            # lint + format
python-barcode==0.15.1 # testlerde Code128 üretimi (opsiyonel)
